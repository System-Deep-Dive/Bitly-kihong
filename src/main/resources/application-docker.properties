# Docker Swarm 환경 설정
# 이 프로파일은 Docker Swarm 환경에서 사용됩니다

# PostgreSQL Datasource
spring.datasource.url=jdbc:postgresql://postgres:5432/bitly

# Redis Configuration
spring.data.redis.host=redis
spring.data.redis.port=6379

# JPA/Hibernate (프로덕션)
spring.jpa.show-sql=false
