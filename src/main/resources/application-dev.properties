# 로컬 개발 환경 설정
# 이 프로파일은 로컬 개발 시 사용됩니다 (기본값)

# PostgreSQL Datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/bitly

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=6380

# JPA/Hibernate (개발 편의)
spring.jpa.show-sql=true
