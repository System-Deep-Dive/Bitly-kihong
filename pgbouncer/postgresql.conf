# PostgreSQL Configuration File
# Docker Swarm 환경에서 PostgreSQL 성능 최적화 설정

# 네트워크 설정 (필수)
listen_addresses = '*'
port = 5432

# 연결 설정
max_connections = 200

# 메모리 설정
shared_buffers = 512MB
effective_cache_size = 1536MB

